This folder contains information about the Evaluation of the Fine-Tuned LLMs and their comparison. In order 
to compare the models, I use the following metrics:

- BERT-score: capture the semantic similarity
- Match@3: checks if at least 1 model suggestion is valid
- Precision@3: checks the proportion of the valid model suggestions

So far, the best model is the one trained on 5 Epochs!