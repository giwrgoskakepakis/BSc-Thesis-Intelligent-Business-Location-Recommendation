These are Houstis data generated from chatgpt. Since they are wrong (unless i change them manually) and they are very few in total, i will use the datasets of this folder to try to SIMULATE the UNIFYING of the data as a practice for later implementation on real world scale data.

- I processed the "restaurant" and "hotel" datasets in order to have corresponding collumns
- I managed to merge them (easy since the columns were the same)

I skipped doing this for the Yelp and Foursquare GPT-Generated datasets because there was no point, i performed the simulation earlier and this was good enough
